# SmartLab React Frontend

React 19 + Vite interface for students, instructors, and administrators. The production Dockerfile compiles this application and copies the output into Laravel's public directory.

```bash
npm install
npm run dev
npm run lint
npm run build
```
