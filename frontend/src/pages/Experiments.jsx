import { useEffect, useState } from "react";import { ArrowRight, Clock3, FlaskConical, Search, SlidersHorizontal } from "lucide-react";import { Link } from "react-router-dom";import api from "../services/api";
export default function Experiments(){const [items,setItems]=useState([]),[search,setSearch]=useState("");useEffect(()=>{api.get("/experiments").then(r=>setItems(r.data))},[]);const filtered=items.filter(x=>(x.title+x.category+x.summary).toLowerCase().includes(search.toLowerCase()));return <><div className="page-title"><div><span className="eyebrow">EXPERIMENT CATALOGUE</span><h1>Explore virtual laboratories</h1><p>Complete the preparation sequence, unlock the simulator, and document your findings.</p></div></div><div className="catalog-toolbar"><div className="search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search experiments or categories…"/></div><button className="button ghost"><SlidersHorizontal/>All disciplines</button></div><div className="experiment-grid">{filtered.map(e=><Link className="experiment-card" to={`/experiments/${e.slug}`} key={e.id}><div className={`card-visual ${e.accent}`}><div className="molecule"><i/><i/><i/><i/></div><span className="difficulty">{e.difficulty}</span><FlaskConical/></div><div className="card-content"><span className="eyebrow">{e.category}</span><h3>{e.title}</h3><p>{e.summary}</p><div className="card-meta"><span><Clock3/> {e.estimated_minutes} min</span><span>{e.manuals_count} manual</span><span>{e.videos_count} video</span></div><div className="card-link">Open laboratory <ArrowRight/></div></div></Link>)}</div></>}
